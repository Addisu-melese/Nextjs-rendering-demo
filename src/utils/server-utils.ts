import "server-only"
export const ServerSideFunction=()=>{
    console.log(`
       use multiple libraries,
       use enviroment libraries,
       intract with database,
       process confidential information
        `)
    return "Server Result";
}